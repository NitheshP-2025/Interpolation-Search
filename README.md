# Interpolation-Search
Interpolation search is a highly efficient algorithm for finding targets in sorted, uniformly distributed arrays. It estimates the item's likely position using its value, narrowing the search space faster than binary search.
